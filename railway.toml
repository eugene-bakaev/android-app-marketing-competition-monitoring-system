[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"
dockerBuildTarget = "api"

[deploy]
startCommand = "packages/api/node_modules/.bin/prisma migrate deploy && node packages/api/dist/index.js"
healthcheckPath = "/api/apps"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
